// Entorno de DESARROLLO
// Cuando Vanessa ejecuta en local (ng serve), Angular usa este fichero
export const environment = {
  production: false,
  // URL del backend de Brandon corriendo en local
  apiUrl: 'http://localhost:8080/api'
};
